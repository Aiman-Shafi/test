let number2 = -34.985665;

// console.log(Math.round(number2)) // to round a number 
// console.log(Math.ceil(number2)) // to increase the value upto the next upper integer
// console.log(Math.floor(number2)) // keep the current integer
// console.log(Math.abs(number2)) // keep the current integer

// generate a random number 

// console.log(Math.random()) // generate a random number between 0 and 1

// generate a random number 1 to 10

var randomNumber = Math.floor(Math.random() * 10) + 1
// console.log(randomNumber)

// generate a random number 1 to 6

var dice = Math.floor(Math.random() * 6) + 1
// console.log(dice)

let text = 5;
text  = text + 5
text  = text + 10
// text--
// text--
// text++

console.log(text)

